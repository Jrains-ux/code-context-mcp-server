## TECH-02 proposal

Implement the first local Bootstrap build that turns a fixed, scoped Python source revision into a validated published snapshot. The change adds admission, AST extraction, staging records, conflict/coverage gates, expected-parent validation, and a local CLI without implementing later Query, Mining, or Sync capabilities.
